---
prev:
  text: '什么是覆盖索引'
  link: '/interview/mysql/08_covering_index'
next:
  text: '索引的数据结构是怎么样的'
  link: '/interview/mysql/10_index_structure'
---

# 最左前缀匹配原则

## 题目

> 什么是最左前缀匹配原则？

## 参考答案

联合索引会从最左列开始匹配，必须尽量连续使用左侧列。

比如索引 `(a,b,c)`：

- 用 `a`、`a,b`、`a,b,c` 都比较好
- 只用 `b,c` 一般不能充分利用索引

所以联合索引设计时，字段顺序非常重要。
## 回答展开

- SQL 和索引题的关键是解释为什么快、为什么慢，而不是只背优化口诀。
- 回答时最好结合执行计划、扫描行数、回表次数和写入成本。
- 真正有经验的回答通常会强调以执行计划和真实数据分布为准。

## 实际场景

比如后台订单列表查询慢，通常要先看执行计划，再判断是不是没走索引、回表太多或深分页导致扫描成本过高。





